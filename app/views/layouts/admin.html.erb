<!DOCTYPE html>
<html>
<head>
   <%= stylesheet_link_tag('application', :media => 'all') %>
  <title>ADC | <%= @page_title || "Admin" %></title>
  <style>
    span.status { display: block; width: 10px; height: 10px;
      margin: 0.25em auto; padding: 0; border: 1px solid #000000; }
    span.status.true  { background: green; color: green; }
    span.status.false { background: red; color: red; }
    </style>
</head>
<body>
  <div id="header">
   <nav class="navbar navbar-inverse" role="navigation">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">ADC - ADMIN Page</a>
      <ul class="nav navbar-nav">
        <li><%= link_to("Menu",{:controller => 'access'}) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a><%= session[:username]%></a></li>
      </ul>  
    </div>
   </nav>
 </div>

  <div class="container">
    <% if !flash[:notice].blank? %>
      <div class="alert alert-success alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>
    
  </div>
  </br></br>
  <div id="footer">
    <nav class="navbar-inverse navbar-fixed-bottom" role="navigation">
      <div class="container">
        <%= render 'layouts/footer' %>
      </div>
    </nav>
  </div>
</body>
</html>
